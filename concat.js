function solve(name, age) {
    let output = `My name is ${name} and my age is ${age + 5}`;
    console.log(output);
}

solve('Joro', 30);
solve('Gosho', 40);
solve('Pesho', 50);

let name = "Joro";
let num = 8;
let output = name + num;
console.log(output);