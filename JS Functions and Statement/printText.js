// 1.
let printText = function (text) {
    console.log(text);
}

let text = 'Gosho';

printText(text);

// 2.
const printText2 = function (text) {
    return text;
}

console.log(printText2('Joro'));